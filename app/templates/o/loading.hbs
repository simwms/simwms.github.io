<div id="dashboard">
  <div class="header-overlay">
    <div class="container">
      <div class="row">
        <div class="col s4 m3 l2 offset-s8 offset-m9 offset-l10">
          <div class="dashboard-avatar z-depth-2 card">
            {{#link-to "o.dashboard.accounts" classNames="card-image"}}
              <img src="images/waifu.jpg">
              <span class="card-title">{{currentUser.user.username}}</span>
            {{/link-to}}
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="dashboard-contents container">
    <div class="row">
      <div class="col s12 m10 l9">
        <ul class="tabs z-depth-1">
          {{#link-to "o.dashboard.accounts" tagName="li" classNames="tab col s4"}}
            <a class="pointer">Warehouses</a>
          {{/link-to}}
          {{#link-to "o.dashboard.payment" tagName="li" classNames="tab col s4"}}
            <a class="pointer">Payment</a>
          {{/link-to}}
          {{#link-to "o.dashboard.preferences" tagName="li" classNames="tab col s4"}}
            <a class="pointer">Preferences</a>
          {{/link-to}}
        </ul>
      </div>
    </div>
    <div class="row">
      <div class="col s12">
        {{md-loader}}
      </div>
    </div>
  </div>
</div>

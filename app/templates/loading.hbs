<div id="dashboard">
  <div class="header-overlay">
    <div class="container">
      <div class="row">
        <div class="col s4 m3 l2 offset-s8 offset-m9 offset-l10">
          <div class="dashboard-avatar z-depth-2 card">
            {{#link-to "dashboard.accounts" classNames="card-image"}}
              <img src="images/waifu.jpg">
              <span class="card-title">{{currentUser.user.username}}</span>
            {{/link-to}}
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="dashboard-contents container">
    <div class="row">
      <div class="col s12 m10 l9">
        <ul class="tabs z-depth-1">
          {{#link-to "dashboard.accounts" tagName="li" classNames="tab col s6"}}
            <a class="pointer">Warehouses</a>
          {{/link-to}}
          {{#link-to "dashboard.preferences" tagName="li" classNames="tab col s6"}}
            <a class="pointer">Preferences</a>
          {{/link-to}}
        </ul>
      </div>
    </div>
    {{#material-fancy-loader class="row" isBusy=true}}
      <h4>Finished Loading</h4>
    {{/material-fancy-loader}}
  </div>
</div>
